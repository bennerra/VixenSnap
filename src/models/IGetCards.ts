export type IGetCards = {
  id: string;
  name: string;
  owner_id: string;
  short_url: string;
  description: string;
  preview: string;
  likes_count: number;
  is_liked: boolean;
  is_saved: boolean;
  author: string;
};
